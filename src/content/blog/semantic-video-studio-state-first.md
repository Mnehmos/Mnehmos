---
title: "Semantic Video Studio: State Is the Product, Pixels Are Just Output"
description: "A control-first video pipeline where four JSON planes compile to a deterministic Blender render, every frame is provenance-stamped, and 'edit the video' means 'patch the state.'"
pubDate: 2026-05-17
author: "Vario (Mnehmos)"
tags: ["AI", "video", "architecture", "state-first", "blender", "provenance", "vibe-coding"]
lesson: "Generated media becomes editable when state is the primary artifact. Pixels are output. State is the product."
---

# Semantic Video Studio: State Is the Product, Pixels Are Just Output

**The video is not the artifact.**

Most AI video generation treats the MP4 as the goal. You write a prompt, you get pixels, you are done. If you want a different camera angle, you prompt again. If you want to change the lighting, you prompt again. If you want to reuse an asset from a previous scene, you prompt again and hope.

This is not a workflow problem. It is an architecture problem. When the output is the only artifact, there is nothing to edit. There is only regeneration.

Semantic Video Studio (SVS) exists to fix the architecture.

---

## The Thesis

> Video should be compiled from durable cinematic state, not hallucinated as disposable pixels.

SVS is a control-first pipeline. A natural-language prompt enters. A four-plane JSON state pack is produced. Blender renders the state pack into frames. ffmpeg muxes the frames into an MP4. A provenance manifest records the SHA-256 hash of every input and output.

The MP4 is the last thing produced. It is also the least important thing stored.

---

## The Four-Plane State Pack

Every SVS project is defined by four JSON files, each validated against a JSON Schema before the render begins:

| Plane | File | Role |
|---|---|---|
| Scene graph | `scene_graph.json` | Object IDs, transforms, cameras, lights, environment |
| Asset manifest | `asset_manifest.json` | Reusable typed assets with interfaces, capabilities, and anchors |
| Timeline | `timeline.json` | Typed beats and actions over object, camera, and light IDs |
| Render plan | `render_plan.json` | Engine, resolution, FPS, samples, output path, render settings |

A render is a **pure function** of these four planes plus the Blender renderer. Same inputs, same output. Every time.

This is the key architectural decision. Render purity means:
- **Partial regeneration** — change one plane, only re-render what changed
- **Asset reuse** — the manifest holds typed, anchored assets with interfaces; they compose across projects
- **Diff and patch** — semantic edits produce typed `SemanticEditPatch` records with base hashes, not prompt rewrites
- **Audit** — the manifest records exactly what state produced which output

---

## The Pipeline

```
Natural-language prompt
  → prompt_to_brief.py       (NL → typed ProductionBrief)
  → brief_to_state_pack.py   (Brief → 4-plane JSON)
  → validate_scene.py        (schemas + cross-refs + writability check)
  → build_video.py           (validate → render → manifest)
  → blender/render_scene.py  (bpy: state → scene → keyframes → PNG sequence)
  → ffmpeg                   (PNG sequence → MP4)
  → outputs/manifests/       (SHA-256 provenance record)
```

The validator runs before Blender opens. Every `object_id` referenced in a timeline action must exist in the scene graph. Every `asset_id` must exist in the manifest. Timeline beats must cover `[0, duration]` without gaps. Output directories must be writable. If any check fails, the render does not start.

This is not defensive programming. It is contract enforcement. The model produces state. The validator decides whether that state is coherent before committing a render job.

---

## The Basics Gate

Seven invariants that the system keeps green forever:

```
test_00_environment.py     — Python version, Blender available, ffmpeg available
test_01_state_render.py    — a valid state pack renders to a non-empty MP4
test_02_plane_regen.py     — modifying one plane re-renders only affected frames
test_03_mcp_roundtrip.py   — MCP tool calls produce valid state mutations
test_04_semantic_patch.py  — NL edit → patch → apply produces valid output state
test_05_prompt_to_state.py — NL prompt → ProductionBrief → state pack is schema-valid
test_06_negative_fixtures.py — invalid state packs are rejected before render
```

These are not unit tests for internal functions. They are integration gates for the pipeline's core claims. If `test_01` fails, renders are broken. If `test_06` fails, the validation layer has regressed. The gate runs on every change.

The Vibe Coders Bible calls these "reflexes." Tests that fire before bad state reaches the system boundary. SVS has seven of them for the render path alone.

---

## The Import Gate

External 3D assets — downloaded from Polyhaven, Sketchfab, or generated by AI — pass through four stages before they can appear in a production:

```
import_external_asset.py      → stage 1: fetch and verify
normalize_imported_asset.py   → stage 2: Blender headless normalize (center, scale, clean)
validate_imported_asset.py    → stage 3: schema validation against asset_record.schema.json
preview_imported_asset.py     → stage 4: headless render preview
imported_asset_report.py      → orchestrator: run all stages, emit AssetImportReport
```

An asset that fails normalization does not enter the registry. An asset that fails validation does not enter the registry. An asset that produces a broken preview does not enter the registry. The registry only holds assets that have cleared all four gates.

This matters because the alternative is discovering, mid-render, that an imported asset has malformed geometry, misaligned pivots, or material slots that Blender cannot resolve. The import gate catches these failures at registration time, not render time.

---

## Semantic Editing Without Regeneration

The standard AI media workflow: describe the change, regenerate everything, compare the result to what you wanted, repeat.

SVS's editing workflow: describe the change, produce a typed patch, validate the patch against the current state's base hash, apply the patch, re-render only what changed.

```python
# semantic_edit_to_patch.py output
{
  "patch_id": "edit_001",
  "base_hash": "a3f8c2...",
  "target_plane": "timeline",
  "target_path": "$.beats[2].camera.position",
  "from_value": [0, 5, 10],
  "to_value": [0, 8, 12],
  "rationale": "pull camera back for wider establishing shot"
}
```

The base hash check is the key mechanism. If the state has changed since the patch was generated, the patch is rejected. You cannot accidentally apply an edit designed for a different version of the scene. This is the same invariant that makes semantic version control possible in code.

---

## What SVS Looks Like in Practice

The `examples/` directory holds the full progression:

```
examples/
  alien_rover/              ← base scene: Mars rover on rocky terrain
  alien_rover_m2_forked/    ← fork at milestone 2
  alien_rover_v002_asset    ← asset import example
  alien_rover_v002_timeline ← timeline-only edit
  alien_rover_v003_edit_001 ← semantic patch applied
  mars_greenhouse/          ← second scene: greenhouse on Mars surface
  imported_asset_demo/      ← scene built around an M11-imported asset
  prompt_suite/results/     ← 5 generated state packs from NL prompts
```

Each subdirectory holds the four-plane JSON for that version of the scene. The progression is legible. You can diff `alien_rover/` against `alien_rover_v003_edit_001/` and see exactly what changed and why.

This is the software engineering discipline applied to media production: the repo is the memory, the state is the source of truth, and the output is a build artifact.

---

## The Provenance Manifest

Every render writes a manifest to `outputs/manifests/<render_id>.json`:

```json
{
  "render_id": "r_20260517_alien_rover",
  "timestamp": "2026-05-17T14:23:11Z",
  "inputs": {
    "scene_graph": { "path": "...", "sha256": "a3f8c2..." },
    "asset_manifest": { "path": "...", "sha256": "b7d1e4..." },
    "timeline": { "path": "...", "sha256": "c9a2f8..." },
    "render_plan": { "path": "...", "sha256": "d5b3e1..." }
  },
  "output": {
    "path": "outputs/alien_rover_r001.mp4",
    "sha256": "e2c7a9...",
    "frame_count": 240,
    "duration_s": 10.0
  },
  "renderer": "blender-5.1",
  "partial_regen": false
}
```

Given a manifest, you can reproduce the render exactly. Given a modified state pack, you can verify which inputs changed and re-render only the affected frames. Given a question about where an output came from, you can answer it.

This is what "provenance" means in practice. Not a note in a README. A machine-readable record that binds every output to the exact inputs that produced it.

---

## The Deeper Pattern

SVS is a case study in a principle that shows up throughout the Mnehmos ecosystem: **the model describes, the engine validates**.

In RPG-MCP, the model narrates combat; the engine enforces hit points and legal moves. In Clio, the model proposes entity updates; the citation auditor enforces source provenance. In LLM Chess, the model proposes moves; the chess engine enforces legality.

In SVS, the model proposes scene state; the validator enforces schema correctness and cross-plane consistency. The render is deterministic once the state is accepted.

The pattern is always the same. The model's job is to describe intent at a level of abstraction where its strengths (language, composition, reasoning about structure) apply. The engine's job is to own ground truth in the domain where rules are deterministic.

Neither can do the other's job well.

---

**Lesson:** Generated media becomes editable when state is the primary artifact. A prompt is not a source file. A four-plane JSON pack is. Pixels are output. State is the product.

**Repo:** [github.com/Mnehmos/semantic-video-studio](https://github.com/Mnehmos/semantic-video-studio)
